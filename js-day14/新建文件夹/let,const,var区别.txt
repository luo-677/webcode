let, const, var的区别：
1.var: var定义的变量，可以跨块访问, 不能跨函数访问。
2.let: let定义的变量，只能在块作用域里访问，不能跨块访问，也不能跨函数访问，存在暂时性死区。
3.const: const用来定义常量，使用时必须初始化(即必须赋值)，只能在块作用域里访问，而且不能修改this指向，存在暂时性死区。
// var 拥有两种作用域 全局作用域 和 函数作用域
// 在预编译阶段 将变量申明提前 赋值为undefined

// let 变量申明 是块级作用域
// 块级  在{} 内有效
// let 不允许重复的变量申明
// let 没有申明提前

// const 用于申明常量
// 申明的常量只允许赋值一次
// const 用于申明常量 命名规范 和变量相同
// const 一般用于外部的模块引入
// const  也是块级作用域
// 没有申明提前